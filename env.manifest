# This file declares all env vars used by this package/application.
# It can be turned into an `.env` file by providing values for the declared env vars.
# Note that not all env vars need to be specified as code may include defaults for their values.
AWS_ACCOUNT_ID # 12-digit AWS account ID hosting your resources (e.g., 123456789012)
AWS_SSO_REGION # Region where AWS SSO (IAM Identity Center) is configured (e.g., us-east-1)
AWS_SSO_START_URL # AWS SSO Start URL from Identity Center (e.g., https://your-company.awsapps.com/start)
AWS_PROFILE # AWS CLI profile name to use with SSO-derived credentials (e.g., acme-sso)